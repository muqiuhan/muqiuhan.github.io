@keyframes fadeIn
    from
        opacity 0
    to
        opacity 1

.fade-in
    animation fadeIn 0.6s ease

.post-list-item, .post-gallery-item
    transition opacity $transition-duration ease
    
    &:hover
        opacity 0.85